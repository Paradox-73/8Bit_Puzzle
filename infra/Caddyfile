# Caddy auto-fetches a free Let's Encrypt HTTPS certificate for SITE_DOMAIN and forwards all
# traffic to the api container. No cert files to manage — it just works once SITE_DOMAIN points
# at this VM's public IP and ports 80 + 443 are open.
{$SITE_DOMAIN} {
	reverse_proxy api:8080
}
